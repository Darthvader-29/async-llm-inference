# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# --- uv / virtualenvs ---
.venv/
.uv/

# --- Tooling caches ---
.ruff_cache/
.mypy_cache/
.pytest_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# --- Secrets / local env ---
.env
.env.*
# Re-include the documented example (gitignore has no inline comments, so the
# negation must be a bare pattern on its own line).
!.env.example

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.idea/
.vscode/
